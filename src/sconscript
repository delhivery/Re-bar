import os

Import('env')
env = env.Clone()

projectLibs = ['ep_graph', 'rc_graph', 'sp_graph', 'ep_mongo']

libraries = list()

for subdir in projectLibs:
    print('Building> ' + subdir);
    library = env.SConscript(
        '{folderName}/sconscript'.format(folderName=subdir),
        {'env': env}
    )
    print('Adding library: {}'.format(library))
    libraries.append(library)

env.Append(LIBS=libraries)

projects = ['solver']

for subdir in projects:
    print('Building>' + subdir)
    env.SConscript(
        '{folderName}/sconscript'.format(folderName=subdir),
        {'env': env}
    )
